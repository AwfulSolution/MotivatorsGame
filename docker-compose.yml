services:
  motivator-game:
    build:
      context: .
    image: motivator-game:latest
    ports:
      - "8080:8080"
    volumes:
      - motivator-data:/data
    restart: unless-stopped

volumes:
  motivator-data:
